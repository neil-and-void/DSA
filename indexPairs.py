from typing import *

class TrieNode:
    def __init__(self, value):
        pass

def indexPairs(text: str, words: List[str]) -> List[List[int]]:
    # build trie

    # iterate from i to len(text) and check if word starts at i
        # if it does, and word exists from i, append [i, len(foundWord)] to answers
    pass

text = ''
words = []
ans = indexPairs(text, words)
print(ans)

